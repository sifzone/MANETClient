pipeline:
    build:
        image: maven:3.3.3-jdk-8-onbuild
        pull: true
        script: "mvn package -B"
        artifacts:
            paths:
                - ManetClient/
        commands:
            - mvn install -q -DskipTests=true
            - mvn test